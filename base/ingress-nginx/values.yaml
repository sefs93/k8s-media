ingress-nginx:
  controller:
    hostPort:
      enabled: true
    kind: DaemonSet
    metrics:
      enabled: true
    #extraArgs:
    #  default-ssl-certificate: "argocd/argocd-tls-cert"
    #  enable-ssl-passthrough: true
    resources:
      limits:
        cpu: 100m
        memory: 90Mi
      requests:
        cpu: 100m
        memory: 90Mi
